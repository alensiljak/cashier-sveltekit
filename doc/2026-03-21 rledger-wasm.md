# RustLedger WASM Implementation

The route `/rledger` contains a demo page for RustLedger WASM integration.

## TODO

- [X] Add text area on top with the editable Beancount source.
- [ ] Add a button to import the current Journal into the text area, as the ledger source.
- [ ] Add Refesh buttons for each demo, that re-runs the functionality, to confirm that it runs from WASM and not JS fallback.
- [ ] Add 'validate' section with a button for manuall validation and display for validation result.
