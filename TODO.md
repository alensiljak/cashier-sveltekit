# Tasks

- [ ] Default to `*` for transaction flag

## OPFS

- [ ] try `showDirectoryPicker()` for uploading a whole directory to OPFS.

## Cashier Server Rust

- [ ] Adapt the parser for the JSON response of the Accounts sync
